import { addI18n } from '@tencentcloud/uikit-base-component-vue3';

const zhCN = {
  Cancel: '取消',
  Accept: '接受',
};

const enUS = {
  Cancel: 'Cancel',
  Accept: 'Accept',
};

addI18n({
  zhCN,
  enUS,
});
