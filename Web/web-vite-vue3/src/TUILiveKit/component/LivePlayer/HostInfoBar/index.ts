// Barrel entry for the top-left host info bar (Douyin-style pill: exit
// button + streamer avatar / name / viewer count / follow / more).
// Internal implementation is a single SFC; kept in its own folder so it
// aligns with the AudiencePanel / MessagePanel domain grouping.
import HostInfoBar from './HostInfoBar.vue';

export default HostInfoBar;
export { HostInfoBar };
